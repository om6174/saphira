import { createApp } from 'vue';
import App from './components/App/App.vue';  // Ensure App.vue is in src/
import "./styles/common.css"


createApp(App).mount('#app');  // Mount the app to the #app div in the HTML
